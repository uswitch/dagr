package main

import (
  "fmt"
  "os/exec"
  "os"
)

func main() {
  cmd := exec.Command("echo", "$PATH"))
  
  cmd.Env = os.Environ()
  fmt.Println("cmd.Env")
  fmt.Println(cmd.Env)
  out, _ := cmd.Output()

  fmt.Println(string(out))
 }